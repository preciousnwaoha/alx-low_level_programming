#include "main.h"
#include <stdlib.h>

/**
 * _abs - prints absolute value of number
 * @n: The number in question
 *
 * Return: Always 0 (Success)
 */
 int _abs(int n)
 {
	 _putchar(abs(n));
	 return (0);
 }
